<div class="cmp-card my-3">@* custom because template is not right *@
  <div class="card card-teaser lightgrey-bg-b1 shadow-sm">
    <div class="card-body">@* custom because template is not right *@
      <div class="card-title">
        <h2 class="title-xxlarge mb-1">@T["Title AccessToActPart"]</h2>
      </div>

      <div class="cmp-info-button-card" >
        <div class="card card-teaser p-3 p-lg-4">
          <div class="card-body pt-1">
            <div>
              @foreach (var shape in Model)
              {
                  @await DisplayAsync(shape)
              }
            </div>
          </div>
        </div>
      </div>

    </div>
  </div>
</div>
